<nav class="top-0 right-0 w-2/3">
    <div class="hidden md:flex items-center justify-end space-x-12 pr-12">
        <a href="{{ path('main_home') }}">Home</a>
        <a href="{{ path('event_list') }}">Events</a>
        {% if app.user %}
            {% if 'ROLE_ADMIN' in app.user.roles %}
                <a href="{{ path('app_place_index') }}">Places</a>
                <a href="{{ path('app_city_index') }}">Cities</a>
            {% endif %}
            <a href="{{ path('user_details', {'id': app.user.id }) }}">{{ app.user.nickname }}</a>
            <a href="{{ path('app_logout') }}">Logout</a>
        {% else %}
            <a href="{{ path('app_register') }}">Register</a>
            <a href="{{ path('app_login') }}">Login</a>
        {% endif %}
    </div>
</nav>